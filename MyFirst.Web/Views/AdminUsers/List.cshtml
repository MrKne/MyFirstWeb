@model MyFirst.Web.Models.ViewModels.UserViewModel
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> signInManager
@inject UserManager<IdentityUser> userManager
@{
}


<div class="bg-secondary bg-opacity-10 py-2">
    <div class="container">
        <h1> Users - Admin Funcionality </h1>
    </div>
</div>

<div> 
<div class="container py-5">
@*    <div class="mb-3 d-flex justify-content-end">

            <button type="button" class="btn btn-dark" data-bs-toggle="modal" data-bs-target="#modalCreateUser" >
                Create new User
            </button>
        </div>*@

      



    @if (Model.Users is not  null || Model.Users.Any())
    {
            
    <table class="table">
        <thead>
            <th class="h5">Preview user Table</th>
                   
            <tr>
                <th> Id </th>
                <th> Username</th>
                <th> Email </th>
                <th></th>
            </tr>
        </thead>
                <tbody>
                    @foreach (var user in Model.Users)
                    {
                     <tr>
                    <td>@user.Id</td>
                    <td>@user.UserName</td>
                    <td>@user.EmailAdress</td>
                    <td>
                               
                        <form method="post" asp-controller="AdminUsers" asp-action="Delete" asp-route-id="@user.Id"> 
                            <button class="btn btn-danger" type="submit"> Delete </button>
                        </form>

                        
                    </td>
                    </tr>
                    }
                </tbody>


                <div class="container mb-3 py-5">   
                    <h1 class="justify-content-center"> Create New User</h1>
                    <form method="post" asp-controller="AdminUsers" asp-action="List">



                    <label class="form-label">Username</label>
                    <input type="text" class="form-control" asp-for="Username" />
                    <label class="form-label">Password</label>
                    <input type="password" class="form-control" asp-for="Password" />
                    <label class="form-label">Email</label>
                    <input type="email" class="form-control" asp-for="Email" />
                    <div class="mb-3 py-5">
                            <div class="mb-3">
                                <div class="form-check">
                                    <input type="checkbox" class="form-check-input" id="chkUserRole" value="User" checked disabled />
                                    <label class="form-check-label" for="chkUserRole"> User Role </label>
                                </div>

                            </div>

                            @if (User.IsInRole("Developer"))
                            {
                                <div class="form-check">
                                    <input type="checkbox" class="form-check-input" id="chkAdminRole" asp-for="AdminRoleCheckBox" />
                                    <label class="form-check-label" for="chkAdminRole"> Admin Role </label>
                                </div>
                            }
                    <button type="submit" class="btn btn-primary" method="post">Save changes</button>
                    </div>
                    </form>
                </div>

                   
           

                    @*  <div class="modal fade" id="modalCreateUser" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                <div class="modal-dialog">
                <div class="modal-content">
                <div class="modal-header">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Create New User </h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                <div class="mb-3">
                <label class="form-label">Username</label>
                <input type="text" class="form-control" asp-for="Username" />
                </div>
                <div class="mb-3">
                <label class="form-label">Email Adress</label>
                <input type="email" class="form-control" asp-for="Email" />
                </div>

                <div class="mb-3">
                <label class="form-label">Password</label>
                <input type="password" class="form-control" asp-for="Password" />
                </div>

                <div class="mb-3">
                <div class="form-check">
                <input type="checkbox" class="form-check-input" id="chkUserRole" value="User" checked disabled />
                <label class="form-check-label" for="chkUserRole"> User Role </label>
                </div>

                </div>

                @if (User.IsInRole("Developer"))
                {
                <div class="form-check">
                <input type="checkbox" class="form-check-input" id="chkAdminRole" asp-for="AdminRoleCheckBox" />
                <label class="form-check-label" for="chkAdminRole"> Admin Role </label>
                </div>
                }

                </div>


                </div>
                <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="submit" class="btn btn-primary" method="post">Save changes</button>
                </div>
                </div>
                </div>

                *@


              


        
    </table>
        }
        else {
        <p> No Users Registered!</p>
        }
</div>

</div>





   

