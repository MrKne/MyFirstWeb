@model MyFirst.Web.Models.ViewModels.HomeViewModel
@{
    ViewData["Title"] = "Home Page";
}

@*<div class="container col-xxl-8 px-4 py-5">
    <div class="row align-items-center g-5 py-5">
        
        <div class="col-12 col-lg-6"> 
        <h1 class="display-6 fw-bold lh-1 mb-3 text-decoration-underline text-danger"> 
          НЕМА ИМЕ УШТЕ МУ МИСЛИМЕ!
        </h1>    
        <p class="lead">
                Добредојдовте на платформата која ви овозможувава лесно и брзо да најдете најдобриот занаетчиски професионалец за вашите потреби! 
                На нашиот вебсајт, ќе најдете разнообразни огласи за секаков вид на занаетчиски услуги, од молери и бравари, до електричари и гиспери. 
                Со нашата широка селекција на огласи, можете да изберете најдобриот професионалец за вашиот проект, базиран на нивната експертиза и искуство. 
                Ние го поврзуваме вашите потреби со најквалитетните занаетчиски услуги, обезбедувајќи ви удобност, безбедност и сигурност во изборот на вашиот следен професионален партнер.
        </p>
        </div>



        <div class="col-12 col-lg-6">
            <img src="https://images.pexels.com/photos/3760613/pexels-photo-3760613.jpeg?auto=compress&cs=tinysrgb&w=600"
                 alt="webphoto-advert" class="d-block mx-lg-auto img-fluid" width="600" />
        </div>

      </div>


</div>
*@

<div class="container px-4 py-5">
    <div class="row justify-content-center">
        <div class="col-6 d-flex">
            @if (Model!=null && Model.Tags.Any())
            {
                foreach (var tag in Model.Tags)
                {
                    {
                        <span class="badge bg-dark me-1"> @tag.Name</span>

                    }
                }
            }
        </div>

    </div>
    </div>


<div class="container-fluid"> 
    <div class="row justify-content-center">
        <div class="col-6 container-fluid">

            @if (Model != null && Model.AdvertPosts.Any())
            {
                foreach (var advertPost in Model.AdvertPosts)
                {
                    <div id="advertIndex" class="mb-5 p-5 box-shadow"> 

                        <img src="@advertPost.FeaturedImageUrl" alt="@advertPost.Heading" class="mb-2 d-block img-fluid" />
                        <h2 class="mb-4"> @advertPost.Heading </h2>
                        <p class="mb-4"> @advertPost.Author <br/>

                            Published Date: @advertPost.PublishedDate.ToShortDateString()</p>

                            <p class="mb-4">
                            @foreach (var tag in advertPost.Tags)
                            {
                                <span class="badge bg-secondary me-1"> @tag.Name</span>


                            }
                            </p>

                        <p class="mb-4"> @advertPost.ShortDescription </p>
                        <a class="btn btn-dark" 
                        asp-area=""
                        asp-controller="Adverts"
                        asp-action="Index"
                        asp-route-urlHandle="@advertPost.UrlHandle"> Read More </a>
                       
                    </div>
                }
            }


        </div>
    </div>
</div>